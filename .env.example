# Porta do servidor (ex.: 3000 na Hostinger)
PORT=3000

# Base de dados (MySQL)
DB_HOST=localhost
DB_PORT=3306
DB_USER=seu_user
DB_PASSWORD=seu_password
DB_NAME=u150959679_evo_leads

# OpenAI – chave em https://platform.openai.com/api-keys (não commitar chave real)
OPENAI_API_KEY=your-openai-api-key-here
# Modelo (opcional; default: gpt-4o-mini)
# OPENAI_MODEL=gpt-4o-mini

# Evolution API – URL base (ex.: http://72.60.45.216:8080)
EVOLUTION_API_URL=http://72.60.45.216:8080
# Mesma apikey usada no header ao configurar o webhook (não commitar chave real)
EVOLUTION_API_KEY=your-evolution-apikey-here
# Nome da instância (ex.: DiegoWoo)
EVOLUTION_INSTANCE=DiegoWoo
# Só ativa a IA quando a mensagem for exatamente esta palavra (opcional; default: financiamento)
# EVO_TRIGGER_WORD=financiamento

# URL base usada para gerar links de upload no ia-app
UPLOAD_BASE_URL=https://ia.rafaapelomundo.com
